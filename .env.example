# SmartApply Human-in-the-Loop Configuration

# === CORE DATABASE (REQUIRED) ===
SUPABASE_URL=your_supabase_url_here
SUPABASE_KEY=your_supabase_anon_key_here

# === HUMAN-IN-THE-LOOP FLAGS ===
# Core feature flags for human-loop mode
USE_CLAUDE_FALLBACK=false
ENABLE_SLACK=false
ENABLE_AIRTABLE=false

# Calibration settings
MATCH_THRESHOLD=0.10

# === JOB INGESTION (REQUIRED) ===
# At least one API key needed for job fetching
LEVER_API_KEY=your_lever_key_here
# GREENHOUSE_API_KEY=not_required_uses_public_api

# === EXTERNAL INTEGRATIONS (DISABLED) ===
# Only required when ENABLE_SLACK=true
# SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
# SLACK_CHANNEL_ID=C1234567890

# Only required when ENABLE_AIRTABLE=true  
# AIRTABLE_API_KEY=your_airtable_key
# AIRTABLE_BASE_ID=your_base_id

# === SCHEDULING ===
CHECK_INTERVAL_MINUTES=15

# === LOGGING ===
LOG_LEVEL=INFO
LOG_FILE=job_application_system.log

# === RUNTIME PROFILES ===
# Current: Human-loop (minimal deps, no LLM calls)
# Future: Hybrid/LLM (when flags enabled)